import { describe, it, expect } from 'vitest';

    describe('Testes de Matemática Básica', () => {
      it('deve somar 1 + 1 corretamente', () => {
        const soma = 1 + 1;
        expect(soma).toBe(2);
      });
    });